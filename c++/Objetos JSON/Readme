Estos proyectos en C++ muestran cómo crear y manipular objetos JSON usando la biblioteca nlohmann/json. Permiten almacenar información de usuario como nombre, apellido, edad y correo electrónico, y luego extraerla para generar un mensaje formateado en consola. El primer ejemplo utiliza una función get_data() para procesar los datos, mientras que el segundo hace la extracción directamente en main(). Ambos sirven como demostración simple y clara de manejo de JSON y manipulación de strings en C++.
